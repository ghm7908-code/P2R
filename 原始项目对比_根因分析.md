# Point2Roof 原始项目 vs P2R 修改版：Building3D 复现失败根因对比分析

## 一、核心发现：坐标系尺度灾难（根因）

### 1.1 原始数据 vs Building3D 数据格式对比

| 属性 | 原始 simulateRoof 数据 | Building3D 数据 |
|------|------------------------|-----------------|
| 坐标系 | **局部坐标系**（米） | **UTM 全球坐标系** |
| 示例 X | 4.2507829804262025 | 535243.880000 |
| 示例 Y | 2.457229847317423 | 6580871.440000 |
| 示例 Z | 1.3325140306207284 | 43.410000 |
| 额外列 | 无（仅 xyz） | RGB + 强度（共 8 列） |
| 数值范围 | ~0–10（三轴均衡） | X~53万, Y~658万, Z~43（**极度失衡**） |

### 1.2 归一化逻辑追踪

**两个版本的归一化代码完全一致**（`_square_bounds` 方法）：

```python
# roofn3d_dataset.py 第 380-386 行（P2R 修改版）
# roofn3d_dataset.py 第 80-85 行（原始版）
@staticmethod
def _square_bounds(points):
    min_pt = np.min(points, axis=0)
    max_pt = np.max(points, axis=0)
    min_xyz = np.min(min_pt)   # 三轴最小值中的最小值（标量）
    max_xyz = np.max(max_pt)   # 三轴最大值中的最大值（标量）
    return np.full(3, min_xyz), np.full(3, max_xyz)
```

**对于原始 simulateRoof 数据**（坐标范围 ~0–5）：
```
min_pt = [0.1, 0.2, 0.0]
max_pt = [4.8, 5.0, 3.2]
min_xyz = 0.0
max_xyz = 5.0
归一化后: X ∈ [0.02, 0.96], Y ∈ [0.04, 1.0], Z ∈ [0.0, 0.64]
→ 三轴均在 [0, 1] 范围内，比例保留 ✅
```

**对于 Building3D 数据**（UTM 坐标）：
```
min_pt = [535200, 6580800, 40]
max_pt = [535300, 6580900, 50]
min_xyz = 40          ← Z 的最小值
max_xyz = 6580900     ← Y 的最大值
归一化范围 = 6580900 - 40 = 6580860

归一化后:
X = (535243.88 - 40) / 6580860 = 0.0813
Y = (6580871.44 - 40) / 6580860 = 0.99996
Z = (43.41 - 40) / 6580860 = 0.000000518  ← ⚠️ Z 几乎为零！

→ Z 维度信息被完全压缩，整个建筑变成了一张二维平面 ❌
```

### 1.3 连锁影响分析

Z 维度被压缩到 ~1e-6 量级后，**所有依赖空间距离的模块失效**：

| 模块 | 参数 | 原始空间含义 | Building3D 上的实际效果 |
|------|------|------------|----------------------|
| PointNet2 SA1 | radius=0.1 | 覆盖 10% 的空间 | X/Y 方向覆盖 10%，Z 方向覆盖 10% × 1e-6 = 几乎无 Z 覆盖 |
| PointNet2 SA4 | radius=0.8 | 覆盖 80% 的空间 | Z 方向仍然几乎为 0 |
| PosRadius | 0.15 | 关键点偏移预测半径 | Z 方向偏移无法学习 |
| DBSCAN eps | 0.05 | 聚类邻域半径 | Z 方向无聚类效果 |
| MatchRadius | 0.2→0.5 | 匈牙利匹配半径 | 在被压缩的空间中，所有点距离极近，匹配失去判别力 |
| refined.clamp | [0.0, 1.0] | 限制预测范围 | Z 方向预测值本来就 ≈0，clamp 无影响但也不修复 |

**一句话总结**：UTM 坐标的 Y 轴（~658 万）比 Z 轴（~43）大了约 **15 万倍**，各向同性归一化后 Z 维度信息被彻底抹除，模型实际在一个**二维平面**上工作。

---

## 二、参数变更对比（原始 → P2R 修改版）

| 参数 | 原始值 | 修改值 | 变化影响 |
|------|--------|--------|----------|
| **NPOINT** | 1024 | 4096 | 点数增加 4 倍，但归一化问题未解决，只是让二维平面更密集 |
| **ScoreThresh** | **0.5** | **0.02** | ⚠️ 从 50% 置信度门槛降到 2%，几乎所有点都通过筛选 → FPS 从 ~4000 个候选点选 16 个，大量噪声进入关键点 |
| **MatchRadius** | **0.2** | **0.5** | ⚠️ 匹配半径扩大 2.5 倍，在已压缩的空间中更失去判别力 |
| **DBSCAN min_pts** | 5 | 2 → FPS 替代 | 从严格聚类变为 FPS 选点，但 FPS 在二维平面上选点效果差 |
| **lr** | 0.001 | 0.002 | 学习率翻倍，可能加剧训练不稳定 |
| **epochs** | 90 | 150 | 训练更久但无法弥补输入数据的缺陷 |
| **edge_start_epoch** | >5 | >3 | 边网络更早启动，但关键点还没学好就启动边训练会引入更多噪声 |
| **num_keypoints** | 无限制（DBSCAN 决定） | 16 | ⚠️ 固定上限，复杂建筑不够用 |
| **Focal Loss alpha** | 0.75 | 0.25 | 正负样本权重更平衡 |
| **pos_weight** | 5.0 | 2.0 | 正样本权重降低 |
| **Loss 归一化** | 除以正样本数 | 除以 batch_size | 修复了 loss scale 问题 ✅ |

### 2.1 ScoreThresh 从 0.5 降到 0.02 的后果

原始设计中 `ScoreThresh=0.5` 意味着：只有当 PointNet2 预测某点是关键点的置信度 > 50% 时，该点才会被送入下游聚类/精炼网络。这是一个合理的过滤门槛。

修改后 `ScoreThresh=0.02` 意味着：4096 个点中约 4000 个点都会通过阈值，FPS 从这 4000 个点中选 16 个。由于 Z 维度已坍缩，FPS 实际上是在一个二维平面上选点——选出的 16 个点大概率不是真正的屋顶角点，而是平面上的随机分散点。

### 2.2 MatchRadius 从 0.2 扩大到 0.5 的后果

在 [0,1] 归一化空间中，`MatchRadius=0.2` 意味着预测点必须在真值点 20% 范围内才算匹配。扩大到 0.5 后，只要预测点在真值点 50% 范围内就匹配成功——这几乎等于"只要在同一个建筑上就算匹配"，匈牙利匹配失去了精度意义。

---

## 三、数据流全链路对比

### 3.1 原始项目数据流
```
simulateRoof/points.xyz (3列，局部坐标 0-5米)
    ↓
read_pts() → 返回全部列 (3列)
    ↓
GaussianTransform (sigma=0.005-0.010, clip=10, p=0.8)
    ↓
随机采样到 1024 点
    ↓
_square_bounds: min_xyz=0, max_xyz=5 → 归一化到 [0,1]³  ✅ 三轴均衡
    ↓
points, vectors 同步归一化
    ↓
PointNet2 (radius=[0.1,0.2,0.4,0.8])  →  在 [0,1]³ 中正常工作  ✅
    ↓
ScoreThresh=0.5 → 只保留高置信度点  ✅
    ↓
DBSCAN (eps=0.05, min_pts=5) → 聚类出关键点  ✅
    ↓
HungarianMatcher (MatchRadius=0.2) → 精确匹配  ✅
    ↓
EdgeAttentionNet → 边预测
```

### 3.2 P2R 修改版数据流
```
Building3D/xxx.xyz (8列，UTM坐标 X~53万 Y~658万 Z~43)
    ↓
read_pts() → pts[:, :3] 截取前3列  (截取正确 ✅，但坐标范围未处理)
    ↓
GaussianTransform (sigma=0.005-0.010, clip=0.05, p=0.8)
    ↓ 震摄 sigma=0.005 的噪声 vs 坐标值 535243 → 噪声影响 ≈0
    ↓
随机采样到 4096 点
    ↓
_square_bounds: min_xyz=40, max_xyz=6580900
    ↓ 归一化: Z = (43-40)/6580860 ≈ 4.6e-6  ⚠️ Z 维度坍缩！
    ↓
points, vectors 同步归一化 (vectors 的 Z 也被压缩)
    ↓
PointNet2 (radius=[0.1,0.2,0.4,0.8])
    ↓ Z 方向覆盖范围 = 0.1 × 4.6e-6 ≈ 0 → ball query 无法捕获 Z 方向邻居
    ↓ 实际上所有 ball query 退化为 2D 圆形查询
    ↓
ScoreThresh=0.02 → 几乎所有点通过  ⚠️
    ↓
FPS 从 ~4000 个二维平面点中选 16 个  ⚠️ 选点质量极差
    ↓
HungarianMatcher (MatchRadius=0.5)
    ↓ 在被压缩的空间中，所有点距离极近，匹配无意义  ⚠️
    ↓
refined.clamp(0.0, 1.0) → Z 方向本来就 ≈0，clamp 无影响
    ↓
EdgeAttentionNet → 基于错误的顶点预测边  ⚠️ 级联失败
```

---

## 四、Building3DReconstructionDataset 存在但未被使用

项目中有一个 `building3d.py` 文件，其中的 `Building3DReconstructionDataset` 类使用了**正确的归一化方式**：

```python
# building3d.py 第 171-178 行
if self.normalize:
    centroid = np.mean(point_cloud[:, 0:3], axis=0)
    point_cloud[:, 0:3] -= centroid           # 去中心化
    max_distance = np.max(np.linalg.norm(point_cloud[:, 0:3], axis=1))
    point_cloud[:, 0:3] /= max_distance       # 按最大欧氏距离归一化
    
    wf_vertices -= centroid
    wf_vertices /= max_distance
```

这种方式归一化后：
- X: (535243 - centroid_x) / max_dist ≈ [-1, 1] 范围
- Y: (6580871 - centroid_y) / max_dist ≈ [-1, 1] 范围
- Z: (43 - centroid_z) / max_dist ≈ [-1, 1] 范围（**三轴均保留了有效信息** ✅）

但 `data_utils.py` 第 90 行始终使用 `RoofN3dDataset`：
```python
dataset = RoofN3dDataset(data_path, transform, data_cfg, logger)
```

`model_cfg.yaml` 中 `use_building3d_format: false`，确认从未激活 Building3D 的数据格式。

**这意味着正确的归一化方案已经存在于代码库中，但从未被接入训练流程。**

---

## 五、根因总结与修复建议

### 5.1 根因优先级排序

| 优先级 | 根因 | 影响 | 修复方式 |
|--------|------|------|----------|
| **P0-致命** | UTM 坐标的各向同性归一化导致 Z 维度坍缩 | 整个模型退化为 2D 操作，屋顶三维结构信息完全丢失 | 改用 Building3D 的 centroid+max_distance 归一化，或对 UTM 坐标先做局部化 |
| **P0-致命** | ScoreThresh 从 0.5 降到 0.02 | 几乎所有点通过筛选，FPS 在 2D 平面上随机选点 | 恢复为 0.3-0.5，或根据验证集分数分布重新校准 |
| **P1-严重** | MatchRadius 从 0.2 扩大到 0.5 | 匹配失去判别力 | 恢复为 0.2 或更低 |
| **P1-严重** | Building3DReconstructionDataset 未被使用 | 正确的归一化方案已存在但未接入 | 切换数据加载路径 |
| **P2-中等** | NPOINT 从 1024 增到 4096 | 内存消耗增加，但未解决根本问题 | 在修复归一化后可保留 4096 |
| **P2-中等** | GaussianTransform 的 clip 从 10 降到 0.05 | 在 UTM 坐标下噪声几乎无影响，但归一化后 clip=0.05 可能过大 | 归一化后 clip 应保持在 0.01-0.02 |

### 5.2 最小修复路径（不改架构）

```
Step 1: 在 RoofN3dDataset.__getitem__ 中，将 _square_bounds 替换为 Building3D 的 centroid+max_distance 归一化
        → 恢复 Z 维度信息

Step 2: ScoreThresh 恢复为 0.3-0.5
        → 减少噪声点进入 FPS

Step 3: MatchRadius 恢复为 0.2
        → 恢复匹配精度

Step 4: refined.clamp 从 [0.0, 1.0] 改为 [-1.0, 1.0]（适配 centroid+max_distance 归一化的范围）
        → 避免截断有效预测

Step 5: 用少量 epoch（10-20）验证 loss 曲线是否正常下降
```

### 5.3 修复后的预期坐标分布

```
Building3D UTM 坐标: X=535243, Y=6580871, Z=43.41
    ↓ centroid 去中心化
X'=535243-535250=-7, Y'=6580871-6580850=21, Z'=43.41-42=1.41
    ↓ / max_distance (假设 max_dist≈25)
X''=-0.28, Y''=0.84, Z''=0.056
    ↓
三轴均在 [-1, 1] 范围内 ✅，Z 维度信息保留 ✅
```
