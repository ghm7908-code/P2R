# 代码修复总结

## 修复日期: 2026-07-22

## 修改文件清单

### 1. `dataset/roofn3d_dataset.py` — 归一化逻辑（核心修复）

**变更**: 将 `_square_bounds` 方法替换为 `_normalize_bounds`

**原因**: Building3D 使用 UTM 坐标系（X~53万, Y~658万, Z~43），旧的各向同性归一化取三轴全局最小/最大值做标量归一化，导致 Z 维度被压缩到 ~1e-6 量级，整个建筑变成二维平面。

**新逻辑**:
```python
centroid = np.mean(points, axis=0)
centered = points - centroid
max_dist = np.max(np.linalg.norm(centered, axis=1))
min_pt = centroid
max_pt = centroid + np.full(3, max_dist)
```

**兼容性**: 返回的 (min_pt, max_pt) 满足现有归一化/反归一化公式：
- 归一化: `(points - min_pt) / (max_pt - min_pt)` → 落在 [-1, 1] 球内
- 反归一化: `normalized * (max_pt - min_pt) + min_pt` → 恢复原始坐标
- `minMaxPt` 存储格式不变，test_util.py 中的反归一化代码无需修改

### 2. `model_cfg.yaml` — 参数校准

| 参数 | 旧值 | 新值 | 原因 |
|------|------|------|------|
| ScoreThresh | 0.02 | 0.3 | 0.02 几乎不过滤任何点，FPS 从 4000+ 候选中选 16 个噪声点 |
| MatchRadius | 0.5 | 0.2 | 0.5 在 [-1,1] 空间中等于 25% 的总范围，匹配失去判别力 |

### 3. `model/cluster_refine.py` — clamp 范围

**变更**: `refined.clamp(0.0, 1.0)` → `refined.clamp(-1.0, 1.0)`

**原因**: 新归一化将坐标映射到 [-1, 1] 范围，旧的 [0, 1] clamp 会截断所有负坐标预测。

### 4. `test.py` — 评估阈值

| 参数 | 旧值 | 新值 | 原因 |
|------|------|------|------|
| point_thresh | 0.1 | 0.2 | 归一化范围从 [0,1] 变为 [-1,1]，总范围翻倍 |
| ap_distance_thresh | 0.1 | 0.2 | 同上 |

---

## 数值验证结果

使用 Building3D UTM 坐标模拟（X span=50m, Y span=50m, Z span=10m）：

```
=== OLD _square_bounds ===
X norm span: 0.00000756
Y norm span: 0.00000757
Z norm span: 0.00000152  <<< Z 维度坍缩
→ SA1 radius=0.1 覆盖整个建筑 13000 次，ball query 失效

=== NEW _normalize_bounds ===
X norm span: 1.44865098
Y norm span: 1.45026665
Z norm span: 0.29052319  <<< Z 维度保留
→ SA1 radius=0.1 覆盖约 7% 的 X 跨度，合理
→ Round-trip error: 0.00e+00 (反归一化精确)
```

---

## 重新训练前的检查清单

1. **删除旧 checkpoint** — 旧模型基于坍缩的 Z 维度训练，权重不可复用
2. **确认 `model_cfg.yaml` 中 ScoreThresh=0.3, MatchRadius=0.2**
3. **确认 `cluster_refine.py` 中 clamp(-1.0, 1.0)**
4. **建议先用 10-20 个 epoch 验证 loss 曲线**
5. **观察初始 cls_loss 是否在 2-5 范围（之前修复过 loss 归一化 Bug）**
