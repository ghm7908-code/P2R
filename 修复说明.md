# Point2Roof 训练问题修复说明

## 已修复的问题

### 问题1：Loss 归一化错误（最关键）

**文件：`model/pointnet2.py` 第 124 行 `get_cls_loss`**

**原代码：**
```python
N_pos_gt = positives.sum().item()
cls_loss = cls_loss_src.sum() / max(N_pos_gt, 1)
```

**问题：** 当正样本极少（~3/4096点）时，将所有点的 focal loss 总和除以 3，导致 loss scale 被放大约 1365 倍。这使得：
- 初始 cls_loss 从应有值 ~2.8 被放大到 ~977（log_9785）甚至 ~43973（log_9796）
- 梯度被过度放大，训练不稳定
- 不同数据子集（N_pos 不同）的 loss scale 差异巨大

**修复：** 改为 `batch_size` 归一化
```python
B = pred.shape[0]
cls_loss = cls_loss_src.sum() / max(B, 1)
```

---

### 问题2：Focal Loss α 值过高

**文件：`model/pointnet2.py` 第 32 行**

**原代码：** `SigmoidFocalClassificationLoss(gamma=2.0, alpha=0.75)`

**问题：** α=0.75 使正样本权重 = 0.75，负样本权重 = 0.25。再配合 pos_weight=5.0，正样本实际权重 = 0.75 × 5.0 = 3.75，是负样本的 15 倍。这导致模型过度关注正样本，学到的特征无法泛化。

**修复：** 改为 `alpha=0.25`
- 正样本权重 = 0.25 × 2.0 = 0.5
- 负样本权重 = 0.75 × 1.0 = 0.75
- 比例 ~1:1.5，更加平衡

---

### 问题3：正样本权重过高

**文件：`model/pointnet2.py` 第 129 行**

**原代码：** `cls_weights = torch.where(positives, 5.0, 1.0)`

**修复：** 改为 `cls_weights = torch.where(positives, 2.0, 1.0)`

---

### 问题4：学习率需要相应调整

**文件：`model_cfg.yaml`**

由于 loss scale 变小（从 ~977 降到 ~2.8），相应提高学习率以保持训练效率。

**修复：** `lr: 0.001` → `lr: 0.002`

---

### 问题5：DBSCAN min_pts 过高导致 edge_cls_loss 快速归零

**文件：`model_cfg.yaml` → Cluster.min_pts**

**原值：** `min_pts: 5`

**问题分析：**
- ScoreThresh=0.2 过滤后，每栋建筑只有 2~4 个点通过阈值
- DBSCAN min_pts=5 要求每个簇至少 5 个点 → 所有点被标记为噪声
- get_cluster_pts 返回无效默认值 (-10,-10,-10)
- HungarianMatcher 过滤无效值 → 无匹配 → 无边标签 → edge_cls_loss=0
- edge_loss=0 又导致 ClusterRefineNet 和 EdgeAttentionNet 无法收到梯度，形成恶性循环

**修复：** `min_pts: 5` → `min_pts: 2`

### PointNet2 backward 实现已正确

之前的诊断文档提到 backward 返回 None/()，但查看当前代码，所有关键操作的 backward 都已正确实现：
- `GatherOperation.backward`：使用 `scatter_add_` ✓
- `ThreeNN.backward`：计算距离梯度 ✓  
- `ThreeInterpolate.backward`：使用 `scatter_add_` ✓
- `GroupingOperation.backward`：使用 `scatter_add_` ✓
- `FurthestPointSampling.backward`：返回 None（正确，FPS 是离散操作）
- `BallQuery.backward`：返回 ()（正确，返回索引）

### 梯度流通路径

Loss → cls_fc → shared_fc → Dropout → fp1(ThreeInterpolate.backward) → fp2 → fp3 → fp4 → SA4(GroupingOperation.backward) → SA3 → SA2 → SA1

梯度可以正常流经整个 PointNet2 backbone，backbone 权重会被更新。

### 训练数据量偏小

当前使用 4096 个训练样本，对于 PointNet2 这种深度模型来说偏少，容易过拟合。
建议：
- 增加训练样本数（如果数据集允许）
- 或增加数据增强（当前只有 Gaussian jitter）

---

## 重新训练前的检查清单

1. 删除旧的 checkpoint（避免从错误 loss scale 的模型 resume）
2. 确认 `model_cfg.yaml` 中 lr=0.002
3. 确认代码修改已保存
4. 建议先用少量 epoch（如 10）验证 loss 曲线是否正常

## 预期效果

修复后初始 cls_loss 预期约 2~5（之前是 977~43973），训练过程中 loss 下降应更平滑，模型泛化能力应有明显改善。
