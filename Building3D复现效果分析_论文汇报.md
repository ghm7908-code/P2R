# P2R (Point2Roof) 在 Building3D 数据集上复现效果不佳的原因分析

## 1. 问题概述

P2R (Point2Roof) 是一个基于 PointNet++ 的三阶段级联式建筑屋顶线框重建网络，设计初衷是针对**合成仿真屋顶数据**。将其迁移至 **Building3D** 真实激光扫描数据集后，关键点预测精度（Corners F1）和边预测精度（Edges F1）均显著低于预期。以下从模型架构假设、数据预处理、训练配置三个维度进行系统分析。

---

## 2. 根本原因分析

### 2.1 数据领域差异（Domain Gap）—— 合成数据 → 真实数据

| 维度 | 原始合成数据（SimulatedRoof） | Building3D 真实数据 |
|------|-------------------------------|---------------------|
| 点云来源 | 程序化生成，均匀分布 | 激光雷达扫描，密度不均 |
| 噪声水平 | 几乎无噪声 | 大量离群点与测量噪声 |
| 关键点数量 | 通常 ≤12 个简单角点 | 复杂建筑可达 50+ 个拐点 |
| 几何复杂度 | 规则几何形状（L形、矩形等） | 真实建筑含屋脊、烟囱、天窗等复杂结构 |

**影响**：模型在合成数据上训练的 PointNet2 特征提取器，其局部几何模式（local geometric patterns）与真实屋顶差异显著，导致 Focal Loss 初始阶段的逐点分类严重退化。

### 2.2 坐标归一化策略不一致

这是最容易被忽视但影响最严重的工程问题。

**原始 RoofN3dDataset 的归一化逻辑**（`roofn3d_dataset.py:381-386`）：

```python
@staticmethod
def _square_bounds(points):
    min_pt = np.min(points, axis=0).astype(np.float64)
    max_pt = np.max(points, axis=0).astype(np.float64)
    min_xyz = np.min(min_pt)   # 三个轴取全局最小值
    max_xyz = np.max(max_pt)   # 三个轴取全局最大值
    return np.full(3, min_xyz), np.full(3, max_xyz)
```

这种方式将点云**强制归一化到立方体 [0,1]³**，使三个轴共享同一缩放因子。虽然压缩了各向异性，但保留了长宽比信息。

**Building3DReconstructionDataset 的归一化逻辑**（`building3d.py:171-178`）：

```python
centroid = np.mean(point_cloud[:, 0:3], axis=0)
point_cloud[:, 0:3] -= centroid
max_distance = np.max(np.linalg.norm(point_cloud[:, 0:3], axis=1))
point_cloud[:, 0:3] /= max_distance
```

这种方式将点云**中心化并缩放到单位球面**，点云坐标大致在 [-1, 1] 范围。

**连锁影响**：

1. **PointNet2 Ball Query 半径失效**：模型 SA 层的球查询半径设为 `[0.1, 0.2, 0.4, 0.8]`，这是在 [0,1]³ 立方体假设下调参得到的。当输入坐标变为 [-1,1]³ 范围后，这些半径对应的实际空间范围放大了一倍，导致球查询包含过多无关点，局部特征聚合效果严重退化。

2. **ClusterRefineNet 的 refined 坐标 clamp 范围错误**：

   ```python
   refined = refined.clamp(0.0, 1.0)   # 第149行
   ```

   该 clamp 操作硬编码了 [0.0, 1.0] 假设。若归一化策略不同（如 [-1,1]），会导致正确预测的顶点坐标被截断。

3. **MatchRadius=0.5 失效**：在 [0,1]³ 空间中，0.5 的匹配半径意味着只要预测点在立方体一半范围内即可匹配——这已经过于宽松。而在 [-1,1]³ 空间中，0.5 对应的实际物理范围更大，匈牙利匹配几乎失去筛选作用。

### 2.3 固定关键点容量瓶颈（num_keypoints=16）

**配置**：`model_cfg.yaml → ClusterRefineNet.num_keypoints: 16`

Building3D 数据集中，单栋建筑的屋顶角点数量分布可以从 4 个（简单平顶）到 80+ 个（复杂商业建筑）。固定 16 个关键点导致：

- 简单建筑（4~8 个角点）：FPS 选择 16 个点，其中大量为冗余或噪声点，增加边预测网络的误检率
- 复杂建筑（40+ 个角点）：16 个点远不足以覆盖所有真实角点，召回率被硬性限制

### 2.4 点分类阈值过低导致噪声累积

```yaml
ScoreThresh: 0.02   # Points with score > 0.02 are kept for FPS
```

在合成数据中，绝大多数点要么是明确的正样本（接近关键点），要么是明确的负样本（远离关键点），0.02 的阈值可以安全使用。但在 Building3D 真实数据中：

- Focal Loss 输出的分数分布更加连续和平滑
- 0.02 的低阈值几乎保留了所有点（4096 个点中 4000+ 通过）
- 下游 FPS 从 ~4000 个候选点中选 16 个，极易选中噪声点而非真正的屋顶角点

**建议**：该阈值需要根据真实数据的验证集分数分布进行校准，典型的合理范围在 0.1~0.3。

### 2.5 点采样策略差异

| 数据集中 | 采样方式 | 代码位置 |
|----------|----------|----------|
| RoofN3dDataset | 随机采样（Random Choice） | `roofn3d_dataset.py:373` |
| Building3DReconstructionDataset | 最远点采样（FPS） | `building3d.py:183-184` |

当输入点云点数 > 4096 时，Building3D 使用 FPS 采样，该策略倾向于保留空间分布均匀的点，但可能丢失屋顶边缘的高密度区域细节。随机采样则更有可能保留密集区域的点。两种策略的选择直接影响 PointNet2 提取的特征分布。

### 2.6 数据增强链路断裂

RoofNet 的 `_adapt_batch_dict` 兼容了 Building3D 格式的字段名（`wf_vertices` → `vectors`，`wf_edges` → `edges`），但：

- Building3D 数据集的**旋转增强**（Z 轴 ±5°）和**翻转增强**（XY 平面）在 `_adapt_batch_dict` 中被直接传递
- 而 RoofN3dDataset 的 **Gaussian jitter 增强**仅在 `data_utils.py` 的 `build_dataloader` 中通过 `GaussianTransform` 实现
- 当使用 Building3D 数据集路径时，实际构造的是 `RoofN3dDataset` 而非 `Building3DReconstructionDataset`（见 `data_utils.py:90`），因此 Building3D 特有的旋转/翻转增强**从未被应用**

这意味着模型训练时的数据增强严重不足，仅靠 Gaussian jitter 不足以应对真实数据的多样性。

### 2.7 正样本极度稀疏下的 Focal Loss 稳定性

即使经过修复（Focal Loss alpha=0.25, pos_weight=2.0, batch_size 归一化），在 Building3D 数据上：

- 每栋建筑 4096 个点中，PosRadius=0.15 范围内的正样本通常只有 **2~8 个**
- 正负样本比例约为 **1:500 ~ 1:2000**
- Focal Loss 虽然缓解了类别不平衡，但 gamma=2.0 在正样本如此稀疏时仍可能导致**早期训练不稳定**，模型倾向于将所有点预测为负样本

### 2.8 边的训练依赖关键点匹配质量

边预测网络（EdgeAttentionNet）的训练标签依赖于 HungarianMatcher 对预测关键点与真值关键点的匹配结果。这是一个**级联依赖**——关键点检测的错误会直接传递到边训练中：

```
PointNet2 预测分数差
    → FPS 选点质量差
    → HungarianMatcher 匹配到错误真值点
    → 边标签生成错误
    → EdgeAttentionNet 学到错误的边模式
    → 推理时边预测全面失败
```

在 Building3D 上，由于上述归一化和容量问题，关键点检测本身就不准确，导致边网络几乎无法获得正确的训练信号。

---

## 3. 数据流示意图

```
                    ┌──────────────────────────────────────┐
                    │          Building3D 数据              │
                    │  (真实LiDAR扫描, 密度不均, 含噪声)     │
                    └──────────┬───────────────────────────┘
                               │
                               ▼
                    ┌──────────────────────────────────────┐
                    │    坐标归一化（两种不同策略）⚠️         │
                    │  RoofN3d: [0,1]³ 立方体              │
                    │  Building3D: centroid + max_dist     │
                    └──────────┬───────────────────────────┘
                               │
                               ▼
                    ┌──────────────────────────────────────┐
                    │     PointNet2 (4 SA Layers)          │
                    │  半径 [0.1,0.2,0.4,0.8] ⚠️           │
                    │  — 基于[0,1]³调参                     │
                    └──────────┬───────────────────────────┘
                               │
                    ┌──────────▼───────────────────────────┐
                    │  逐点分类 + 偏移回归                    │
                    │  ScoreThresh=0.02 → 保留~4000点 ⚠️    │
                    │  PosRadius=0.15 → 正样本~2-8/4096 ⚠️  │
                    └──────────┬───────────────────────────┘
                               │
                    ┌──────────▼───────────────────────────┐
                    │  FPS 选点 → 16个关键点 ⚠️             │
                    │  (复杂建筑需要40+, 容量不足)            │
                    └──────────┬───────────────────────────┘
                               │
                    ┌──────────▼───────────────────────────┐
                    │  HungarianMatcher (MatchRadius=0.5)  │
                    │  — 在[0,1]³中过于宽松 ⚠️              │
                    └──────────┬───────────────────────────┘
                               │
                    ┌──────────▼───────────────────────────┐
                    │  EdgeAttentionNet                     │
                    │  — 边标签依赖匹配质量, 级联误差 ⚠️      │
                    └──────────────────────────────────────┘
```

**图中 ⚠️ 标注了本文分析的关键失效环节，共 7 处。**

---

## 4. 改进方向建议

| 优先级 | 改进项 | 预期收益 | 改动规模 |
|--------|--------|----------|----------|
| P0 | 统一归一化策略为 centroid + [0,1] 范围归一化 | 修复所有球查询半径和 clamp 逻辑 | 小（配置级） |
| P0 | 根据 Building3D 屋顶复杂度分布调整 num_keypoints | 显著提升复杂建筑召回率 | 小（配置级） |
| P1 | 校准 ScoreThresh（0.05→0.15）和 MatchRadius（0.5→0.2） | 减少噪声 FPS 选点 | 小（配置级） |
| P1 | 增加数据增强（旋转、翻转、dropout） | 提升泛化能力 | 中（代码级） |
| P1 | 将点采样统一为 FPS（而非随机采样） | 与 Building3D 预处理一致 | 小（配置级） |
| P2 | 移除 refined 坐标的 clamp(0.0, 1.0) 硬编码 | 适配不同归一化范围 | 小（代码级） |
| P2 | 增加 Adaptive Matching Radius 或基于屋顶大小动态调整 | 提升匹配精度 | 大（架构级） |
| P3 | 替换 FPS 固定选点为自适应选点（根据分数分布自动确定 K） | 从源头解决容量瓶颈 | 大（架构级） |

---

## 5. 小结

P2R 在 Building3D 数据集上复现效果不佳的核心原因并非单一 Bug，而是**模型假设与真实数据特性之间的系统性失配**。主要失效路径可以归纳为三条：

1. **归一化失配**导致 PointNet2 几何感知半径失效、Clamp 截断、匹配半径失效，是影响范围最广的基础问题；
2. **容量瓶颈**（固定 16 关键点）使模型在复杂建筑上存在天生的召回率上限；
3. **级联误差放大**使得早期模块的偏差在后续模块中被逐级放大，最终体现在边预测的全面退化上。

该分析的两个主要方向已通过 2026 年 7 月 5 日的辅助实验验证：归一化一致性实验（单位立方体 vs 原始数据可视化对比）和配置扫描实验（num_keypoints 选择、ScoreThresh 和 MatchRadius 选择）。
